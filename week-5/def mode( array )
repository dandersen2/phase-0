def mode( array )
hash = Hash.new(0)
array.each{|key| hash[key] += 1}
  hash.each { |key, val|
       highest = key if val == hash.values.max
            a = [highest]
            print highest
            }
            end
            mode([1,2,2,4,4])